# Heartbeat

This file is checked periodically (default: every 30 minutes).
Add tasks below for the agent to execute automatically.

<!-- Example:
## Check server status
Run `uptime` and `df -h` to monitor the system.
-->
